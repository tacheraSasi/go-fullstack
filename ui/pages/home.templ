package pages

type HomeProps struct{
    AppName string
}
templ Home(props HomeProps) {
<div class="p-4">
    <h1 class="text-2xl font-bold mb-4">Home Page</h1>
    <p>Welcome to the {props.AppName}</p>
</div>
}